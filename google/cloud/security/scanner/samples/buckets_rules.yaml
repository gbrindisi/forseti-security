rules:
  # Only allow Organization users to be in the Organization groups.
  - name: open writable buckets
    mode: blacklist
    resource:
      - type: bucket
        applies_to: self_and_children
        resource_ids:
          - bucket_name
    inherit_from_parents: true
    bindings:
      - entity: allUsers
      - role: OWNER|WRITER
